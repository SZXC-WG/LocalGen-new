#define VER_STRING "${PROJECT_VERSION_MAJOR}.${PROJECT_VERSION_MINOR}.${PROJECT_VERSION_PATCH}.${BUILD_NUMBER}"
#define VER_MAJOR ${PROJECT_VERSION_MAJOR}
#define VER_MINOR ${PROJECT_VERSION_MINOR}
#define VER_RELEASE	${PROJECT_VERSION_PATCH}
#define VER_BUILD ${BUILD_NUMBER}